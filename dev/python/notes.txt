# where global pip packages are for python installed with pyenv
# NOTE: can 'go to definition' for libs that support it to find the location aswell
~/.pyenv/versions/3.11.2/lib/python3.11/site-packages/

# run python file
python ./src/main.rb

# run python command
python -c 'print("hi")
print("there")
'

# list outdated packages
. .venv/bin/activate
pip list -o

# run black formatting on selected lines in vims
'<,'>! black -q -

# If a package comes back that cannot be found in the requirements.txt file it is probably a nested dependency. 
# So run pipdeptree:
pip install pipdeptree
pipdeptree

